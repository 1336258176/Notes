# 类和对象

---

1. 类访问修饰符：
   `public` : 公有成员，类内外均可访问
   `private` : 私有成员，在类外部不可访问和查看，只有友元函数和类本身（实例也不行）可以查看。
   `protected` : 受保护成员，与 `private` 相同，但其可在子类中访问
2. 继承
3. 友元函数：声明在类内（声明前加关键字 `friend` ），定义在类外（非成员函数，不加范围解析），可访问 `private` 和 `pretected` 成员
