# Gitignore
***

* ***忽略特殊文件，将要忽略的文件放到 `.gitignore` 文件中，其配置可查阅GitHub上相关开源项目（用 `-f` 可强制添加忽略的文件）***

* ***用 `! <file>` 可把指定文件排除***

* **命令**
    > `git check-ignore -v <file>`[^1]
    >
    > `git config --global alias.<short_name> <original_name>`[^2]

[^1]: 查看自定义的忽略规则
[^2]: 自定义命令